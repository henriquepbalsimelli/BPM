

export type ProductInterface = {
    id: number
    name: string
    price: number
    quantity: number
    selecterSize: string
    selectedColor: string
    sizes: string[]
    colors: string[]
    description: string
}
